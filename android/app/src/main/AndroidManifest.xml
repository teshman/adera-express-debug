<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="adera.express"> <!-- THIS IS CRUCIAL: MUST MATCH YOUR FIREBASE PACKAGE NAME AND namespace/applicationId IN app/build.gradle.kts -->

    <!-- Essential permissions for React Native apps -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Required for development builds to allow overlay windows for dev menu -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- Add other necessary permissions here as your app needs them (e.g., android.permission.CAMERA) -->

    <application
        android:name=".MainApplication"
    android:label="@string/app_name"
    android:icon="@mipmap/ic_launcher"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:allowBackup="false"
    android:theme="@style/AppTheme"
    android:usesCleartextTraffic="true"> <!-- Required for dev server on Android 9+ (API level 28+) to allow HTTP traffic -->
<!-- android:name=".MainActivity"  Refers to adera.express.MainActivity -->
    <activity
        android:name=".MainActivity" 
    android:label="@string/app_name"
    android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
    android:launchMode="singleTask"
    android:windowSoftInputMode="adjustResize"
    android:exported="true"> <!-- Required for activities launched by the system, like launcher activities -->

    <!-- Intent filter to make this the main launcher activity -->
    <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
    </intent-filter>
</activity>

    <!-- Add any other activities your application might have here -->
    <!-- Example: <activity android:name=".AnotherScreenActivity" android:exported="false" /> -->

    </application>
    </manifest>